"""
Module contains constant values used across the tests.

It defines various constants that are shared among different test cases.
"""

from pathlib import Path

THIS_DIR = Path(__file__).parent
PROJECT_DIR = (THIS_DIR / "../").resolve()
